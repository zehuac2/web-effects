# AGENTS.md

This is an astro project.

- Use `bun` as the package manager
- Styling uses Tailwind CSS v4 (via `@tailwindcss/vite`), configured in `src/styles/global.css`
- Compose class lists with `clsx`, one class per parameter
